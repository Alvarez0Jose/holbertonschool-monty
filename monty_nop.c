#include "monty.h"

/**
 * nop - does nothing in the stack
 * @stack: stack provided in the struct
 * @line_number: line where the code is
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
